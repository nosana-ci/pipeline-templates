{
  "version": "0.1",
  "type": "container",
  "meta": {
    "trigger": "dashboard"
  },
  "ops": [
    {
      "type": "container/run",
      "id": "shap-e-text-to-3d",
      "args": {
        "image": "diffusers/diffusers-pytorch-cuda",
        "cmd": [
          "bash",
          "-c",
          "mkdir -p /output  && python -c \"import torch; from diffusers import ShapEPipeline; from diffusers.utils import export_to_gif; ckpt_id = 'openai/shap-e'; pipe = ShapEPipeline.from_pretrained(ckpt_id).to('cuda'); guidance_scale = 15.0; prompt = 'a shark'; images = pipe(prompt, guidance_scale=guidance_scale, num_inference_steps=64).images; gif_path = export_to_gif(images[0], '/output/a_shark_3d.gif'); print('Generated 3D model saved to', gif_path)\" && python -m http.server 80 --directory /output"
        ],
        "gpu": true
      }
    }
  ]
}